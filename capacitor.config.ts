import { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = {
  appId: "io.ionic.simonetenisci.lastpressed",
  appName: "Last Pressed",
  bundledWebRuntime: false,
  npmClient: "npm",
  webDir: "www",
};

export default config;
